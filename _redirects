# Redirecionamentos para Netlify
# Este arquivo garante que todas as rotas funcionem corretamente

# Permitir acesso direto a recursos estáticos
/images/* /images/:splat 200
/*.css /:splat 200
/*.js /:splat 200
/*.png /:splat 200
/*.jpg /:splat 200
/*.jpeg /:splat 200
/*.gif /:splat 200
/*.ico /:splat 200

# Redirecionar raiz para o manual
/ /Manual_Completo_Sistema_Host.html 200

# Redirecionar qualquer 404 para o manual principal (apenas para páginas)
/* /Manual_Completo_Sistema_Host.html 200 